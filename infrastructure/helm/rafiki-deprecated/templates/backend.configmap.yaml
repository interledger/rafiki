apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.name" . }}
  labels:
{{ include "rafiki.labels" . | indent 4 }}
data: 
  ADMIN_PORT: "{{ .Values.backend.port.admin }}"
  AUTH_SERVER_GRANT_URL: {{ include "auth.grantUrl" . | quote }}
  AUTH_SERVER_INTROSPECTION_URL: {{ include "auth.introspectionUrl" . | quote }}
  CONNECTOR_PORT: "{{ .Values.backend.port.connector }}"
  EXCHANGE_RATES_LIFETIME: "{{ .Values.backend.lifetime.exchangeRate }}"
  EXCHANGE_RATES_URL: "{{ .Values.backend.serviceUrls.EXCHANGE_RATES_URL }}"
  GRAPHQL_IDEMPOTENCY_KEY_TTL_MS: "{{ .Values.backend.idempotency.keyTTL}}"
  GRAPHQL_IDEMPOTENCY_KEY_LOCK_MS: "{{ .Values.backend.idempotency.keyLock}}"
  ILP_ADDRESS: "{{ .Values.backend.ilp.address }}"
  INCOMING_PAYMENT_WORKERS: "{{ .Values.backend.workers.incomingPayment }}"
  INCOMING_PAYMENT_WORKER_IDLE: "{{ .Values.backend.workerIdle }}"
  KEY_ID: "{{ .Values.backend.key.id }}"
  LOG_LEVEL: "{{ .Values.backend.logLevel }}"
  NODE_ENV: "{{ .Values.backend.nodeEnv }}"
  OPEN_PAYMENTS_PORT: "{{ .Values.backend.port.openPayments }}"
  OPEN_PAYMENTS_URL: "{{ .Values.backend.serviceUrls.OPEN_PAYMENTS_URL }}"
  OUTGOING_PAYMENT_WORKERS: "{{ .Values.backend.workers.outgoingPayment }}"
  OUTGOING_PAYMENT_WORKER_IDLE: "{{ .Values.backend.workerIdle }}"
  PAYMENT_POINTER_URL: "{{ .Values.backend.serviceUrls.PAYMENT_POINTER_URL }}"
  PAYMENT_POINTER_WORKERS: "{{ .Values.backend.workers.paymentPointer }}"
  PAYMENT_POINTER_WORKER_IDLE: "{{ .Values.backend.workerIdle }}"
  PRIVATE_KEY_FILE: "{{ .Values.backend.key.file }}"
  PUBLIC_HOST: "{{ .Values.backend.serviceUrls.PUBLIC_HOST }}"
  QUOTE_LIFESPAN: "{{ .Values.backend.lifetime.quote }}"
  QUOTE_URL: "{{ .Values.backend.serviceUrls.QUOTE_URL }}"
  REDIS_TLS_CA_FILE_PATH: "{{ .Values.backend.tlsCaFile }}"
  REDIS_TLS_CERT_FILE_PATH: "{{ .Values.backend.tlsCertFile }}"
  REDIS_TLS_KEY_FILE_PATH: "{{ .Values.backend.tlsKeyFile }}"
  REDIS_URL: {{ include "backend.redisUrl" . | quote }}
  SIGNATURE_VERSION: "1"
  SLIPPAGE: "{{ .Values.backend.ilp.slippage }}"
  TIGERBEETLE_CLUSTER_ID: none
  TIGERBEETLE_REPLICA_ADDRESSES: none
  USE_TIGERBEETLE: "false"
  WEBHOOK_TIMEOUT:  "{{ .Values.backend.lifetime.webhook }}"
  WEBHOOK_URL: "{{ .Values.backend.serviceUrls.WEBHOOK_URL }}"
  WEBHOOK_WORKERS: "{{ .Values.backend.workers.webhook }}"
  WEBHOOK_WORKER_IDLE: "{{ .Values.backend.workerIdle }}"
  WITHDRAWAL_THROTTLE_DELAY: "{{ .Values.backend.withdrawalThrottleDelay }}"