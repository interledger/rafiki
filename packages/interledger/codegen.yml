overwrite: true
schema: "./src/admin-api/schema.graphql"
generates:
  src/admin-api/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      defaultMapper: Partial<{T}>
      resolverTypeWrapperSignature: Promise<T>
      scalars:
        UInt64: bigint
  src/admin-api/generated/graphql.schema.json:
    plugins:
      - "introspection"
