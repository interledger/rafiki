:::note[Tenant ID and HMAC-signed request headers]
The `tenantId` is derived from the HMAC-signed request headers. Include a `tenant-id` header identifying the tenant on whose behalf the request is made.
:::
